#!/usr/bin/env zsh
# ------------------------------------------------------------------------------
# VANILLA LOADER (LEGACY FALLBACK)
# ------------------------------------------------------------------------------
# Role: Manually load components if Sheldon is absent or disabled.
# ------------------------------------------------------------------------------

# TODO: Implement manual loop for legacy custom plugins/files here.
# Example:
# for plugin in "$ZDOTPLUGINS"/*/*.plugin.zsh(N); do
#   source "$plugin"
# done

echo "Warning: Loading in Vanilla mode (Legacy Fallback)."
